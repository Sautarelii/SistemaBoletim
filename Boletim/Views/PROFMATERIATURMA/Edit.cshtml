@model Boletim.PROFMATERIATURMA

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>PROFMATERIATURMA</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.COD_PROF)

        @Html.HiddenFor(model => model.COD_MATERIA)

        @Html.HiddenFor(model => model.COD_TURMA)

        @Html.HiddenFor(model => model.PERIODO_LETIVO)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Salvar" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Voltar", "Index")
</div>
